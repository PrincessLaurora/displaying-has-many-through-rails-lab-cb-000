<h1><%= @doctor.name%></h1><br>
<h2><%= @doctor.department%></h2>

<%@doctor.appointments.each do |appo|%>
 <li><%=appo.appointment_datetime.strftime("%B %-d, %Y at %k:%M")%></li>

 <%=link_to appo.patient.name, patient_path(appo.patient)%>

 <%end%>
